<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="start"
    android:background="@color/primary_colour">

    <ImageView
        android:layout_width="@dimen/nav_header_width"
        android:layout_height="@dimen/nav_header_height"
        android:gravity="start"
        android:layout_marginStart="@dimen/nav_header_margin_start"
        android:layout_marginTop="@dimen/nav_header_margin_above"
        android:layout_marginBottom="@dimen/nav_header_margin_below"
        android:src="@mipmap/drawer_logo"
        android:contentDescription="@string/venture_logo_desc" />

</FrameLayout>